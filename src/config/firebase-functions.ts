/**
 * Región única de las Callable Functions V4.
 *
 * Firestore (default) reside en europe-southwest1; clientes y servidores
 * deben usar esta misma región para evitar la región implícita us-central1.
 */
export const V4_FUNCTIONS_REGION = 'europe-southwest1'